import turtle

# Create a turtle object
t = turtle.Turtle()

# Set the length and angle for your drawing
length = 20
angle = 45

# Use the turtle object to move forward
t.forward(length)

# Keep the window open until the user closes it
turtle.mainloop()
